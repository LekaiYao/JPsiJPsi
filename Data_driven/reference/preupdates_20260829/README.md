# Pre-updates 2026-08-29 regression reference

这里仅保存 H014/H015/H016 已验收结果的小型 JSON/CSV/metadata 和重绘输入。
它们用于检查迁移后的代码是否保留原有功能，不是 ROOT 6.40.02 新 fitting model 的
nominal artifact。历史文件中的 `tests/atlas_data_driven_sps/` 路径是 provenance，
在旧测试目录删除后不会改写。
